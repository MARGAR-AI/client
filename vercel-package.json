{
  "name": "ai-provider-assignment-vercel",
  "version": "1.0.0",
  "description": "Vercel deployment for AI Provider Assignment",
  "scripts": {
    "vercel-build": "cp -r *.csv ./client/build/ && cd client && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parser": "^3.0.0",
    "date-fns": "^2.30.0",
    "express": "^4.18.2",
    "node-cache": "^5.1.2"
  },
  "engines": {
    "node": "^18.0.0"
  }
} 